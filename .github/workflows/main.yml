name: FirstFlow
on:
  pull_request:
    types: [labeled]

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Run a one-line script
        run: echo Hello world!

  deploy-main:
    runs-on: ubuntu-latest
    environment:
      name: Main
      url: "urltest.com"
    steps:
      - name: Run a one-line script
        run: echo Deploy homolog!